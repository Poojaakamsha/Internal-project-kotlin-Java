server:
  port: 9001

spring:
  application:
    name: BANKING-SERVICE-PRO1

eureka:
  client:
    register-with-client: true
    fetch-registry: true
    service-url:
      default:zone: http://localhost:8761/eureka/
    instance:
      hostname: localhost